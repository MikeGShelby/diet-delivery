<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Diet Delivery</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
     <header>
        <h1><a href="/">Diet Delivery</a></h1>
        <h2>Meal delivery for chronic health conditions</h2>
      <nav>
        {{#if loggedIn}}
          <a href="/dashboard">dashboard</a>
          <button id="logout" class="btn-no-style">logout</button>
        {{else}}
          <a href="/login">login</a>
        {{/if}}
      </nav>
    </header>

    <main>
      <section class="buttons">
        <h3>Diet Filters</h3>
        <button type="button" class="upvote-btn">Renal</button>
        <button type="button" class="upvote-btn">Diabetic</button>
        <button type="button" class="upvote-btn">Low FODMAP</button>
      </section>

      {{{ body }}}
    </main>

    <footer>
      Thanks for visiting!
    </footer>
  </div>

  {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>